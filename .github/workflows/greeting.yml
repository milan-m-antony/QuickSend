name: Greeting
on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Message received! Thanks for opening an issue on QuickSend. We will take a look shortly. 🚀'
        pr-message: 'Nice work! Thanks for submitting a PR to QuickSend. We will review it soon.'
